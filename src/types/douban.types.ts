export type ObjectStatus = "done" | "mark" | "doing";

export type ObjectTypes = "movie" | "drama" | "book" | "music" | "game";

export const OBJECT_STATUSES: ObjectStatus[] = ["done", "mark", "doing"];
export const OBJECT_TYPES: ObjectTypes[] = [
    "movie",
    "drama",
    "book",
    "music",
    "game",
];
